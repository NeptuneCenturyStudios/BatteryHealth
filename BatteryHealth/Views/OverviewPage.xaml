<Page
    x:Class="BatteryHealth.Views.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BatteryHealth.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        <Grid Background="{ThemeResource AppBarBackgroundThemeBrush}" Padding="12">

            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            
            <!--Battery indicator-->
            <TextBlock Grid.Row="0" FontFamily="{ThemeResource SymbolThemeFontFamily}" FontSize="56" Text="&#xEBA7;" />
            <!--Status text-->
            <TextBlock Grid.Row="1" Text="Discharging" />
        </Grid>
    </Grid>
</Page>
